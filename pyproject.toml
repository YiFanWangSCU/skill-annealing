[project]
name = "skill-annealing"
version = "0.1.0"
description = "Controlled experiments for robustness under reduced Agent skill prompts."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
